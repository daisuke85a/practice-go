# practice go

## a tour of go

```bash
$ docker-compose up -d
$ docker-compose exec app go run ./a_tour_of_go/{$filename}
```

## web app sample

```bash
$ docker-compose up -d
$ docker-compose exec app go run main.go
```
